from distutils.core import setup

setup(
    name='code-scanner',
    packages=['code-scanner'],
    version='1.0.1',
    description='detector for essential code changes, generate a report after analysis',
    author='wanzih',
    author_email='..',
    url='..',
)
